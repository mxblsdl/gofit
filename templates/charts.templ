package templates

import "github.com/gofit/models"

templ LineChart(data models.ChartData) {
    
    @Layout(data.Title) {
         <h1>{data.Title}</h1>
        <p>{data.Subtitle}</p>
        
        <div id="line-chart-container" style="width:100%; height:500px;"></div>       
    }
}